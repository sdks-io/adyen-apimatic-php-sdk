<?php

declare(strict_types=1);

/*
 * AdyenApIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace AdyenApIsLib\Models;

use AdyenApIsLib\ApiHelper;
use stdClass;

class AdditionalDataRetry implements \JsonSerializable
{
    /**
     * @var string|null
     */
    private $retryChainAttemptNumber;

    /**
     * @var string|null
     */
    private $retryOrderAttemptNumber;

    /**
     * @var string|null
     */
    private $retrySkipRetry;

    /**
     * Returns Retry Chain Attempt Number.
     * The number of times the transaction (not order) has been retried between different payment service
     * providers. For instance, the `chainAttemptNumber` set to 2 means that this transaction has been
     * recently tried on another provider before being sent to Adyen.
     *
     * > If you submit `retry.chainAttemptNumber`, `retry.orderAttemptNumber`, and `retry.skipRetry` values,
     * we also recommend you provide the `merchantOrderReference` to facilitate linking payment attempts
     * together.
     */
    public function getRetryChainAttemptNumber(): ?string
    {
        return $this->retryChainAttemptNumber;
    }

    /**
     * Sets Retry Chain Attempt Number.
     * The number of times the transaction (not order) has been retried between different payment service
     * providers. For instance, the `chainAttemptNumber` set to 2 means that this transaction has been
     * recently tried on another provider before being sent to Adyen.
     *
     * > If you submit `retry.chainAttemptNumber`, `retry.orderAttemptNumber`, and `retry.skipRetry` values,
     * we also recommend you provide the `merchantOrderReference` to facilitate linking payment attempts
     * together.
     *
     * @maps retry.chainAttemptNumber
     */
    public function setRetryChainAttemptNumber(?string $retryChainAttemptNumber): void
    {
        $this->retryChainAttemptNumber = $retryChainAttemptNumber;
    }

    /**
     * Returns Retry Order Attempt Number.
     * The index of the attempt to bill a particular order, which is identified by the
     * `merchantOrderReference` field. For example, if a recurring transaction fails and is retried one day
     * later, then the order number for these attempts would be 1 and 2, respectively.
     *
     * > If you submit `retry.chainAttemptNumber`, `retry.orderAttemptNumber`, and `retry.skipRetry` values,
     * we also recommend you provide the `merchantOrderReference` to facilitate linking payment attempts
     * together.
     */
    public function getRetryOrderAttemptNumber(): ?string
    {
        return $this->retryOrderAttemptNumber;
    }

    /**
     * Sets Retry Order Attempt Number.
     * The index of the attempt to bill a particular order, which is identified by the
     * `merchantOrderReference` field. For example, if a recurring transaction fails and is retried one day
     * later, then the order number for these attempts would be 1 and 2, respectively.
     *
     * > If you submit `retry.chainAttemptNumber`, `retry.orderAttemptNumber`, and `retry.skipRetry` values,
     * we also recommend you provide the `merchantOrderReference` to facilitate linking payment attempts
     * together.
     *
     * @maps retry.orderAttemptNumber
     */
    public function setRetryOrderAttemptNumber(?string $retryOrderAttemptNumber): void
    {
        $this->retryOrderAttemptNumber = $retryOrderAttemptNumber;
    }

    /**
     * Returns Retry Skip Retry.
     * The Boolean value indicating whether Adyen should skip or retry this transaction, if possible.
     *
     * > If you submit `retry.chainAttemptNumber`, `retry.orderAttemptNumber`, and `retry.skipRetry` values,
     * we also recommend you provide the `merchantOrderReference` to facilitate linking payment attempts
     * together.
     */
    public function getRetrySkipRetry(): ?string
    {
        return $this->retrySkipRetry;
    }

    /**
     * Sets Retry Skip Retry.
     * The Boolean value indicating whether Adyen should skip or retry this transaction, if possible.
     *
     * > If you submit `retry.chainAttemptNumber`, `retry.orderAttemptNumber`, and `retry.skipRetry` values,
     * we also recommend you provide the `merchantOrderReference` to facilitate linking payment attempts
     * together.
     *
     * @maps retry.skipRetry
     */
    public function setRetrySkipRetry(?string $retrySkipRetry): void
    {
        $this->retrySkipRetry = $retrySkipRetry;
    }

    /**
     * Converts the AdditionalDataRetry object to a human-readable string representation.
     *
     * @return string The string representation of the AdditionalDataRetry object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'AdditionalDataRetry',
            [
                'retryChainAttemptNumber' => $this->retryChainAttemptNumber,
                'retryOrderAttemptNumber' => $this->retryOrderAttemptNumber,
                'retrySkipRetry' => $this->retrySkipRetry
            ]
        );
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (isset($this->retryChainAttemptNumber)) {
            $json['retry.chainAttemptNumber'] = $this->retryChainAttemptNumber;
        }
        if (isset($this->retryOrderAttemptNumber)) {
            $json['retry.orderAttemptNumber'] = $this->retryOrderAttemptNumber;
        }
        if (isset($this->retrySkipRetry)) {
            $json['retry.skipRetry']          = $this->retrySkipRetry;
        }

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
