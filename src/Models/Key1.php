<?php

declare(strict_types=1);

/*
 * AdyenApIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace AdyenApIsLib\Models;

use AdyenApIsLib\ApiHelper;
use stdClass;

/**
 * The key you share with Adyen to secure local communications when using Terminal API.
 */
class Key1 implements \JsonSerializable
{
    /**
     * @var string|null
     */
    private $identifier;

    /**
     * @var string|null
     */
    private $passphrase;

    /**
     * @var int|null
     */
    private $version;

    /**
     * Returns Identifier.
     * The unique identifier of the shared key.
     */
    public function getIdentifier(): ?string
    {
        return $this->identifier;
    }

    /**
     * Sets Identifier.
     * The unique identifier of the shared key.
     *
     * @maps identifier
     */
    public function setIdentifier(?string $identifier): void
    {
        $this->identifier = $identifier;
    }

    /**
     * Returns Passphrase.
     * The secure passphrase to protect the shared key. Must consist of:
     *
     * * At least 12 characters.
     *
     * * At least 1 uppercase letter: `[A-Z]`.
     *
     * * At least 1 lowercase letter: `[a-z]`.
     *
     * * At least 1 digit: `[0-9]`.
     *
     * * At least 1 special character. Limited to the following: `~`, `!`, `@`, `#`, `$`, `%`, `^`, `&`,
     * `*`, `(`, `)`, `_`, `+`, `=`, `}`, `{`, `]`, `[`, `;`, `:`, `?`, `.`, `,`, `>`, `<`.
     */
    public function getPassphrase(): ?string
    {
        return $this->passphrase;
    }

    /**
     * Sets Passphrase.
     * The secure passphrase to protect the shared key. Must consist of:
     *
     * * At least 12 characters.
     *
     * * At least 1 uppercase letter: `[A-Z]`.
     *
     * * At least 1 lowercase letter: `[a-z]`.
     *
     * * At least 1 digit: `[0-9]`.
     *
     * * At least 1 special character. Limited to the following: `~`, `!`, `@`, `#`, `$`, `%`, `^`, `&`,
     * `*`, `(`, `)`, `_`, `+`, `=`, `}`, `{`, `]`, `[`, `;`, `:`, `?`, `.`, `,`, `>`, `<`.
     *
     * @maps passphrase
     */
    public function setPassphrase(?string $passphrase): void
    {
        $this->passphrase = $passphrase;
    }

    /**
     * Returns Version.
     * The version number of the shared key.
     */
    public function getVersion(): ?int
    {
        return $this->version;
    }

    /**
     * Sets Version.
     * The version number of the shared key.
     *
     * @maps version
     */
    public function setVersion(?int $version): void
    {
        $this->version = $version;
    }

    /**
     * Converts the Key1 object to a human-readable string representation.
     *
     * @return string The string representation of the Key1 object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'Key1',
            ['identifier' => $this->identifier, 'passphrase' => $this->passphrase, 'version' => $this->version]
        );
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (isset($this->identifier)) {
            $json['identifier'] = $this->identifier;
        }
        if (isset($this->passphrase)) {
            $json['passphrase'] = $this->passphrase;
        }
        if (isset($this->version)) {
            $json['version']    = $this->version;
        }

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
