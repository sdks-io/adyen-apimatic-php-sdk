<?php

declare(strict_types=1);

/*
 * AdyenApIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace AdyenApIsLib\Models;

use AdyenApIsLib\ApiHelper;
use stdClass;

class SessionResultResponse implements \JsonSerializable
{
    /**
     * @var array<string,string>|null
     */
    private $additionalData;

    /**
     * @var string|null
     */
    private $id;

    /**
     * @var Payment[]|null
     */
    private $payments;

    /**
     * @var string|null
     */
    private $reference;

    /**
     * @var string|null
     */
    private $status;

    /**
     * Returns Additional Data.
     * Contains additional information about the payment. Some fields are included only if you enable them.
     * To enable these fields in your Customer Area, go to **Developers** > **Additional data**.
     *
     * @return array<string,string>|null
     */
    public function getAdditionalData(): ?array
    {
        return $this->additionalData;
    }

    /**
     * Sets Additional Data.
     * Contains additional information about the payment. Some fields are included only if you enable them.
     * To enable these fields in your Customer Area, go to **Developers** > **Additional data**.
     *
     * @maps additionalData
     *
     * @param array<string,string>|null $additionalData
     */
    public function setAdditionalData(?array $additionalData): void
    {
        $this->additionalData = $additionalData;
    }

    /**
     * Returns Id.
     * A unique identifier of the session.
     */
    public function getId(): ?string
    {
        return $this->id;
    }

    /**
     * Sets Id.
     * A unique identifier of the session.
     *
     * @maps id
     */
    public function setId(?string $id): void
    {
        $this->id = $id;
    }

    /**
     * Returns Payments.
     * A list of all authorised payments done for this session.
     *
     * @return Payment[]|null
     */
    public function getPayments(): ?array
    {
        return $this->payments;
    }

    /**
     * Sets Payments.
     * A list of all authorised payments done for this session.
     *
     * @maps payments
     *
     * @param Payment[]|null $payments
     */
    public function setPayments(?array $payments): void
    {
        $this->payments = $payments;
    }

    /**
     * Returns Reference.
     * The unique reference that you provided in the original `/sessions` request. This identifies the
     * payment and is used in all communication with you about the payment status.
     */
    public function getReference(): ?string
    {
        return $this->reference;
    }

    /**
     * Sets Reference.
     * The unique reference that you provided in the original `/sessions` request. This identifies the
     * payment and is used in all communication with you about the payment status.
     *
     * @maps reference
     */
    public function setReference(?string $reference): void
    {
        $this->reference = $reference;
    }

    /**
     * Returns Status.
     * The status of the session. The status included in the response doesn't get updated. Don't make the
     * request again to check for payment status updates.
     *
     * Possible values:
     * * **completed**: the shopper completed the payment, and the payment was authorized.
     * * **paymentPending**: the shopper is in the process of making the payment. This applies to payment
     * methods with an asynchronous flow, like voucher payments where the shopper completes the payment in
     * a physical shop.
     * * **refused**: the session has been refused, because of too many refused payment attempts. The
     * shopper can no longer complete the payment with this session.
     * * **canceled**: the shopper canceled the payment.
     * * **expired**: the session expired. The shopper can no longer complete the payment with this session.
     * By default, the session expires one hour after it is created.
     */
    public function getStatus(): ?string
    {
        return $this->status;
    }

    /**
     * Sets Status.
     * The status of the session. The status included in the response doesn't get updated. Don't make the
     * request again to check for payment status updates.
     *
     * Possible values:
     * * **completed**: the shopper completed the payment, and the payment was authorized.
     * * **paymentPending**: the shopper is in the process of making the payment. This applies to payment
     * methods with an asynchronous flow, like voucher payments where the shopper completes the payment in
     * a physical shop.
     * * **refused**: the session has been refused, because of too many refused payment attempts. The
     * shopper can no longer complete the payment with this session.
     * * **canceled**: the shopper canceled the payment.
     * * **expired**: the session expired. The shopper can no longer complete the payment with this session.
     * By default, the session expires one hour after it is created.
     *
     * @maps status
     * @factory \AdyenApIsLib\Models\Status4::checkValue
     */
    public function setStatus(?string $status): void
    {
        $this->status = $status;
    }

    /**
     * Converts the SessionResultResponse object to a human-readable string representation.
     *
     * @return string The string representation of the SessionResultResponse object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'SessionResultResponse',
            [
                'additionalData' => $this->additionalData,
                'id' => $this->id,
                'payments' => $this->payments,
                'reference' => $this->reference,
                'status' => $this->status
            ]
        );
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (isset($this->additionalData)) {
            $json['additionalData'] = $this->additionalData;
        }
        if (isset($this->id)) {
            $json['id']             = $this->id;
        }
        if (isset($this->payments)) {
            $json['payments']       = $this->payments;
        }
        if (isset($this->reference)) {
            $json['reference']      = $this->reference;
        }
        if (isset($this->status)) {
            $json['status']         = Status4::checkValue($this->status);
        }

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
